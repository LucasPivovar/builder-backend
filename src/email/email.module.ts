import { Module } from '@nestjs/common';import { TypeOrmModule } from '@nestjs/typeorm';import { AuthModule } from '../auth/auth.module';import { WorkspaceModule } from '../workspace/workspace.module';import { EmailController } from './email.controller';import { EmailCampaignEntity, EmailContactEntity, EmailDeliveryEntity } from './email.entity';import { EmailService } from './email.service';
@Module({imports:[TypeOrmModule.forFeature([EmailContactEntity,EmailCampaignEntity,EmailDeliveryEntity]),AuthModule,WorkspaceModule],controllers:[EmailController],providers:[EmailService]})export class EmailModule{}
