import { Body, Controller, Delete, Get, UseGuards } from '@nestjs/common';import { CurrentUser } from '../auth/current-user.decorator';import { AuthenticatedUser, JwtAuthGuard } from '../auth/jwt-auth.guard';import { DeleteAccountDto } from './privacy.dto';import { PrivacyService } from './privacy.service';
@UseGuards(JwtAuthGuard)@Controller('privacy')export class PrivacyController{constructor(private privacy:PrivacyService){}@Get('export')export(@CurrentUser()u:AuthenticatedUser){return this.privacy.export(u.userId)}@Delete('account')delete(@CurrentUser()u:AuthenticatedUser,@Body()dto:DeleteAccountDto){return this.privacy.deleteAccount(u.userId,dto.password)}}
